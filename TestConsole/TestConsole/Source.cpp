#include <stdio.h>
#include <time.h>
#include <stdlib.h>
int main()
{
	int card[] = { 1, 2, 3, 4, 5, 6, 7, 8, 9};
	char card[4] = { k, q, j };
	srand(time(NULL));
	int card[] = (rand() % 11);
	printf("%c", card);
	for (;;);




}